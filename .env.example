# Environment variables for Idea Vetter
# Not currently needed — reserved for future use (e.g., API keys).
# Copy to .env if/when you need to set values.

# Example: API key for a paid search provider
# SEARCH_API_KEY=your-key-here
